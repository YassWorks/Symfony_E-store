<nav style="display: flex; justify-content: space-between; align-items: center; padding: 1rem; background: #f8f9fa;">
    <div>
        <a href="{{ path('landing_page') }}" style="margin-right: 1rem;">Home</a>
        <a href="#" style="margin-right: 1rem;">Browse</a>
    </div>
    <div>
        {% if user %}
            {% if 'ROLE_SELLER' in user.roles %}
                <a href="{{ path('seller_dashboard') }}" style="margin-right: 1rem;">Your Shop</a>
            {% else %}
                <a href="{{ path('join_us') }}" style="margin-right: 1rem;">Join us!</a>
            {% endif %}

            <a href="{{ path('profile') }}" style="margin-right: 1rem;">Profile</a>
            <a href="{{ path('cart') }}" style="margin-right: 1rem;">Cart</a>
            <a href="{{ path('logout') }}">Logout</a>
        {% else %}
            <a href="{{ path('login') }}">Login</a>
        {% endif %}
    </div>
</nav>